services:
  onlyoffice:
    image: onlyoffice/documentserver:latest
    container_name: scalewise-onlyoffice
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      JWT_ENABLED: "false"
